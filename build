#!/usr/bin/env bash
set -euo pipefail

# As of 0.13, xmonad --recompile will look for a custom build script.

stack build :my-xmonad --verbosity info
stack install :my-xmonad --local-bin-path bin/ --verbosity info
mv bin/my-xmonad "$1"
